export const metadata = {
  title: 'Linktree',
  description: 'Linktree',
}

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
